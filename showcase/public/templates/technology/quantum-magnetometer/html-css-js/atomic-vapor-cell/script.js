const c=document.getElementById('spinCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let t=0;function draw(){ctx.clearRect(0,0,c.width,c.height);for(let i=0;i<8;i++){ctx.beginPath();const x=c.width/2+Math.cos(t+i)*80;const y=c.height/2+Math.sin(t+i)*80;ctx.arc(x,y,4,0,Math.PI*2);ctx.fillStyle='rgba(245,158,11,0.6)';ctx.fill()}t+=.03;requestAnimationFrame(draw)}draw();