const c=document.getElementById('bubbles'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const bubs=Array.from({length:20},()=>({x:Math.random()*c.width,y:c.height+Math.random()*200,r:Math.random()*15+5,vy:Math.random()*1+.5,o:Math.random()*.5+.3}));function draw(){ctx.clearRect(0,0,c.width,c.height);bubs.forEach(b=>{b.y-=b.vy;b.x+=Math.sin(Date.now()*.002+b.r)*.5;if(b.y+b.r<0){b.y=c.height+b.r;b.x=Math.random()*c.width}ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fillStyle='rgba(234,88,12,'+b.o+')';ctx.fill()});requestAnimationFrame(draw)}draw();