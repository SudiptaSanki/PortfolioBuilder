const sc=document.getElementById('stars'),sctx=sc.getContext('2d');const ac=document.getElementById('aurora'),actx=ac.getContext('2d');function resize(){sc.width=ac.width=window.innerWidth;sc.height=ac.height=window.innerHeight;drawStars()}window.addEventListener('resize',resize);resize();
function drawStars(){sctx.clearRect(0,0,sc.width,sc.height);for(let i=0;i<200;i++){sctx.beginPath();sctx.arc(Math.random()*sc.width,Math.random()*sc.height,Math.random()*1.5,0,Math.PI*2);sctx.fillStyle='rgba(255,255,255,'+(Math.random()*.6+.2)+')';sctx.fill()}}
let t=0;function drawAurora(){actx.clearRect(0,0,ac.width,ac.height);for(let i=0;i<3;i++){actx.beginPath();for(let x=0;x<ac.width;x+=5){const y=ac.height*.3+Math.sin(x*.005+t+i*2)*80+Math.sin(x*.01+t*1.5)*40+i*40;if(x===0)actx.moveTo(x,y);else actx.lineTo(x,y)}actx.lineTo(ac.width,0);actx.lineTo(0,0);actx.closePath();const g=actx.createLinearGradient(0,0,ac.width,0);g.addColorStop(0,'rgba(34,197,94,0.15)');g.addColorStop(.5,'rgba(168,85,247,0.15)');g.addColorStop(1,'rgba(34,197,94,0.15)');actx.fillStyle=g;actx.fill()}t+=.015;requestAnimationFrame(drawAurora)}drawAurora();