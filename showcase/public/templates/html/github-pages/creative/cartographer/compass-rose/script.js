const c=document.getElementById('compass'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let angle=0;function draw(){ctx.clearRect(0,0,c.width,c.height);ctx.save();ctx.translate(c.width/2,c.height/2);ctx.rotate(angle);const r=Math.min(c.width,c.height)*.35;for(let i=0;i<8;i++){const a=i*Math.PI/4;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);ctx.strokeStyle='rgba(194,149,106,0.5)';ctx.lineWidth=i%2===0?2:1;ctx.stroke()}ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.strokeStyle='rgba(194,149,106,0.2)';ctx.lineWidth=1;ctx.stroke();ctx.restore();angle+=.002;requestAnimationFrame(draw)}draw();