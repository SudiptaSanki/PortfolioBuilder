const c=document.getElementById('goldCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const flakes=Array.from({length:35},()=>({x:Math.random()*c.width,y:Math.random()*c.height,s:Math.random()*2.5+.5,vy:Math.random()*.3+.1}));function draw(){ctx.clearRect(0,0,c.width,c.height);flakes.forEach(p=>{p.y+=p.vy;if(p.y>c.height)p.y=0;ctx.beginPath();ctx.arc(p.x,p.y,p.s,0,Math.PI*2);ctx.fillStyle='rgba(212,175,55,0.5)';ctx.fill()});requestAnimationFrame(draw)}draw();