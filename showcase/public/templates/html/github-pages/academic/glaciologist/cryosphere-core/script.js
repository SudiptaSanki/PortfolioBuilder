const c=document.getElementById('iceCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const bubs=Array.from({length:40},()=>({x:Math.random()*c.width,y:Math.random()*c.height,r:Math.random()*2+.5,vy:Math.random()*.2+.05}));function draw(){ctx.clearRect(0,0,c.width,c.height);bubs.forEach(b=>{b.y-=b.vy;if(b.y<0)b.y=c.height;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fillStyle='rgba(125,211,252,0.3)';ctx.fill()});requestAnimationFrame(draw)}draw();