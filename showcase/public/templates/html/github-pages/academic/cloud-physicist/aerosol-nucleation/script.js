const c=document.getElementById('iceCrystalCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const crystals=Array.from({length:45},()=>({x:Math.random()*c.width,y:Math.random()*c.height,r:Math.random()*2+.5,vy:Math.random()*.2+.05}));function draw(){ctx.clearRect(0,0,c.width,c.height);crystals.forEach(p=>{p.y+=p.vy;if(p.y>c.height)p.y=0;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(56,189,248,0.35)';ctx.fill()});requestAnimationFrame(draw)}draw();