const c=document.getElementById('rainCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const drops=Array.from({length:60},()=>({x:Math.random()*c.width,y:Math.random()*c.height,l:Math.random()*15+5,vy:Math.random()*8+4}));function draw(){ctx.clearRect(0,0,c.width,c.height);drops.forEach(d=>{d.y+=d.vy;if(d.y>c.height)d.y=0;ctx.beginPath();ctx.moveTo(d.x,d.y);ctx.lineTo(d.x,d.y+d.l);ctx.strokeStyle='rgba(56,189,248,0.4)';ctx.lineWidth=1;ctx.stroke()});requestAnimationFrame(draw)}draw();