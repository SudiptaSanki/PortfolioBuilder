const c=document.getElementById('albedoCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const p=Array.from({length:35},()=>({x:Math.random()*c.width,y:Math.random()*c.height,r:Math.random()*2.5+1,vy:Math.random()*.2+.05}));function draw(){ctx.clearRect(0,0,c.width,c.height);p.forEach(b=>{b.y+=b.vy;if(b.y>c.height)b.y=0;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fillStyle='rgba(56,189,248,0.3)';ctx.fill()});requestAnimationFrame(draw)}draw();