const c=document.getElementById('dropCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const drops=Array.from({length:30},()=>({x:Math.random()*c.width,y:Math.random()*c.height,r:Math.random()*2+1,vy:Math.random()*.5+.2}));function draw(){ctx.clearRect(0,0,c.width,c.height);drops.forEach(d=>{d.y+=d.vy;if(d.y>c.height)d.y=0;ctx.beginPath();ctx.arc(d.x,d.y,d.r,0,Math.PI*2);ctx.fillStyle='rgba(245,158,11,0.4)';ctx.fill()});requestAnimationFrame(draw)}draw();