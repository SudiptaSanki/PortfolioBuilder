const c=document.getElementById('embers'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const sparks=[];function draw(){ctx.fillStyle='rgba(10,6,4,0.15)';ctx.fillRect(0,0,c.width,c.height);if(Math.random()>.85){const x=c.width*.3+Math.random()*c.width*.4;sparks.push({x,y:c.height,vx:(Math.random()-.5)*2,vy:-(Math.random()*4+2),life:1,s:Math.random()*3+1})}sparks.forEach((s,i)=>{s.x+=s.vx;s.y+=s.vy;s.vy+=.03;s.life-=.015;if(s.life<=0){sparks.splice(i,1);return}ctx.beginPath();ctx.arc(s.x,s.y,s.s,0,Math.PI*2);ctx.fillStyle='rgba(249,115,22,'+s.life+')';ctx.fill()});requestAnimationFrame(draw)}draw();
document.addEventListener('mousemove',e=>{const glow=document.querySelector('.forge-glow');glow.style.left=e.clientX-250+'px';glow.style.top=e.clientY-250+'px'});