const c=document.getElementById('eq'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=200}window.addEventListener('resize',resize);resize();const bars=32;function draw(){ctx.clearRect(0,0,c.width,c.height);const bw=c.width/bars;for(let i=0;i<bars;i++){const h=(Math.sin(Date.now()*.003+i*.5)*.5+.5)*c.height*.8;ctx.fillStyle='rgba(245,158,11,'+(0.3+h/c.height*.5)+')';ctx.fillRect(i*bw+2,c.height-h,bw-4,h)}requestAnimationFrame(draw)}draw();