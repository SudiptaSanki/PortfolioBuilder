const c=document.getElementById('leaves'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const particles=Array.from({length:25},()=>({x:Math.random()*c.width,y:Math.random()*c.height,s:Math.random()*3+1,vy:Math.random()*.3+.1,vx:(Math.random()-.5)*.3}));function draw(){ctx.clearRect(0,0,c.width,c.height);particles.forEach(p=>{p.y+=p.vy;p.x+=p.vx;if(p.y>c.height){p.y=0;p.x=Math.random()*c.width}ctx.beginPath();ctx.arc(p.x,p.y,p.s,0,Math.PI*2);ctx.fillStyle='rgba(107,142,35,0.25)';ctx.fill()});requestAnimationFrame(draw)}draw();