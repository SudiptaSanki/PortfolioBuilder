const c=document.getElementById('sonar'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let rings=[];setInterval(()=>{rings.push({r:0,o:1})},2000);function draw(){ctx.clearRect(0,0,c.width,c.height);const cx=c.width/2,cy=c.height/2;rings=rings.filter(r=>r.o>0);rings.forEach(r=>{ctx.beginPath();ctx.arc(cx,cy,r.r,0,Math.PI*2);ctx.strokeStyle='rgba(34,197,94,'+r.o+')';ctx.lineWidth=1.5;ctx.stroke();r.r+=1.5;r.o-=.005});requestAnimationFrame(draw)}draw();