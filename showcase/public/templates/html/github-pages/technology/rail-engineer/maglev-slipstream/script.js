const c=document.getElementById('streamCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const lines=Array.from({length:40},()=>({x:Math.random()*c.width,y:Math.random()*c.height,l:Math.random()*100+50,vx:Math.random()*12+6}));function draw(){ctx.clearRect(0,0,c.width,c.height);lines.forEach(s=>{s.x+=s.vx;if(s.x>c.width)s.x=-s.l;ctx.beginPath();ctx.moveTo(s.x,s.y);ctx.lineTo(s.x+s.l,s.y);ctx.strokeStyle='rgba(59,130,246,0.4)';ctx.lineWidth=1;ctx.stroke()});requestAnimationFrame(draw)}draw();