const c=document.getElementById('pemCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const protons=Array.from({length:40},()=>({x:Math.random()*c.width,y:Math.random()*c.height,r:Math.random()*2+.5,vx:Math.random()*2+1}));function draw(){ctx.clearRect(0,0,c.width,c.height);protons.forEach(p=>{p.x+=p.vx;if(p.x>c.width)p.x=0;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(20,184,166,0.4)';ctx.fill()});requestAnimationFrame(draw)}draw();