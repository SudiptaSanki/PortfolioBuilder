const c=document.getElementById('hydroCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let t=0;function draw(){ctx.clearRect(0,0,c.width,c.height);for(let i=0;i<6;i++){ctx.beginPath();const r=40+i*40+Math.sin(t+i)*10;ctx.arc(c.width/2,c.height/2,r,0,Math.PI*2);ctx.strokeStyle='rgba(34,197,94,0.2)';ctx.stroke()}t+=.02;requestAnimationFrame(draw)}draw();