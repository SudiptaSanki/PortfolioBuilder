const c=document.getElementById('tetherCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let t=0;function draw(){ctx.clearRect(0,0,c.width,c.height);ctx.beginPath();for(let x=0;x<c.width;x+=5){let y=c.height/2+Math.sin(x*.01+t)*30+Math.sin(x*.03+t*1.5)*15;x===0?ctx.moveTo(x,y):ctx.lineTo(x,y)}ctx.strokeStyle='rgba(56,189,248,0.6)';ctx.lineWidth=2;ctx.stroke();t+=.03;requestAnimationFrame(draw)}draw();