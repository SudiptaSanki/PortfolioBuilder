const c=document.getElementById('plasmaCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let t=0;function draw(){ctx.clearRect(0,0,c.width,c.height);const cx=c.width/2,cy=c.height/2;ctx.beginPath();ctx.arc(cx,cy,120+Math.sin(t*2)*15,0,Math.PI*2);ctx.fillStyle='rgba(168,85,247,0.15)';ctx.fill();t+=.03;requestAnimationFrame(draw)}draw();