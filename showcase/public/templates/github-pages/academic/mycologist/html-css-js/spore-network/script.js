const c=document.getElementById('mycelium'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const nodes=Array.from({length:60},()=>({x:Math.random()*c.width,y:Math.random()*c.height}));function draw(){ctx.clearRect(0,0,c.width,c.height);nodes.forEach((a,i)=>{nodes.forEach((b,j)=>{if(j<=i)return;const d=Math.hypot(a.x-b.x,a.y-b.y);if(d<180){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle='rgba(163,230,53,'+(1-d/180)*.3+')';ctx.lineWidth=.5;ctx.stroke()}});ctx.beginPath();ctx.arc(a.x,a.y,2,0,Math.PI*2);ctx.fillStyle='rgba(163,230,53,0.6)';ctx.fill();a.x+=Math.sin(Date.now()*.001+i)*.3;a.y+=Math.cos(Date.now()*.001+i)*.3});requestAnimationFrame(draw)}draw();