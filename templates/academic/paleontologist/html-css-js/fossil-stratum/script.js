const c=document.getElementById('dust'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const motes=Array.from({length:35},()=>({x:Math.random()*c.width,y:Math.random()*c.height,s:Math.random()*2+.5,vx:(Math.random()-.5)*.3,vy:Math.random()*.2+.05}));function draw(){ctx.clearRect(0,0,c.width,c.height);motes.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.y>c.height){p.y=0;p.x=Math.random()*c.width}if(p.x<0||p.x>c.width)p.vx*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.s,0,Math.PI*2);ctx.fillStyle='rgba(194,149,106,0.35)';ctx.fill()});requestAnimationFrame(draw)}draw();
document.addEventListener('scroll',()=>{const s=window.scrollY;document.querySelectorAll('.strata').forEach((el,i)=>{el.style.transform='translateY('+-s*(.05+i*.02)+'px)'})});