const c=document.getElementById('seismic'),ctx=c.getContext('2d');let t=0;function resize(){c.width=window.innerWidth;c.height=120}window.addEventListener('resize',resize);resize();function draw(){ctx.clearRect(0,0,c.width,c.height);ctx.beginPath();for(let x=0;x<c.width;x+=3){let y=60+Math.sin(x*.015+t)*20+Math.random()*4;x===0?ctx.moveTo(x,y):ctx.lineTo(x,y)}ctx.strokeStyle='rgba(255,69,0,0.6)';ctx.lineWidth=1.5;ctx.stroke();t+=.04;requestAnimationFrame(draw)}draw();