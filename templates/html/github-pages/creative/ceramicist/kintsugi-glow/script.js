const c=document.getElementById('cracks'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight;drawCracks()}window.addEventListener('resize',resize);resize();function drawCracks(){ctx.clearRect(0,0,c.width,c.height);for(let i=0;i<6;i++){ctx.beginPath();let x=Math.random()*c.width,y=Math.random()*c.height;ctx.moveTo(x,y);for(let j=0;j<8;j++){x+=(Math.random()-.5)*120;y+=(Math.random()-.5)*120;ctx.lineTo(x,y)}ctx.strokeStyle='rgba(212,175,55,0.3)';ctx.lineWidth=1;ctx.stroke()}}
document.addEventListener('mousemove',e=>{const glow=document.querySelector('.gold-line');if(glow){const r=e.clientX/window.innerWidth*100;glow.style.background='linear-gradient(90deg,transparent '+Math.max(0,r-30)+'%,#d4af37 '+r+'%,transparent '+Math.min(100,r+30)+'%)'}});