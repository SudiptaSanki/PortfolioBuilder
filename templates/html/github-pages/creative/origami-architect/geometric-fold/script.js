const c=document.getElementById('foldCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();function draw(){ctx.clearRect(0,0,c.width,c.height);const sp=100;for(let x=0;x<c.width+sp;x+=sp){for(let y=0;y<c.height+sp;y+=sp){ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+sp/2,y+sp);ctx.lineTo(x+sp,y);ctx.strokeStyle='#334155';ctx.lineWidth=1;ctx.stroke()}}}draw();