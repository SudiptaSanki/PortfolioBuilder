const c=document.getElementById('shimmerCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const pts=Array.from({length:50},()=>({x:Math.random()*c.width,y:Math.random()*c.height,o:Math.random()}));function draw(){ctx.clearRect(0,0,c.width,c.height);pts.forEach(p=>{p.o=Math.sin(Date.now()*.002+p.x)*.4+.5;ctx.beginPath();ctx.arc(p.x,p.y,1.5,0,Math.PI*2);ctx.fillStyle='rgba(212,175,55,'+p.o+')';ctx.fill()});requestAnimationFrame(draw)}draw();