const c=document.getElementById('sunCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let angle=0;function draw(){ctx.clearRect(0,0,c.width,c.height);const cx=c.width/2,cy=c.height/2;ctx.save();ctx.translate(cx,cy);ctx.rotate(angle);for(let i=0;i<12;i++){const a=i*Math.PI/6;ctx.beginPath();ctx.arc(Math.cos(a)*180,Math.sin(a)*180,Math.random()*40+20,0,Math.PI*2);ctx.fillStyle='rgba(255,170,0,0.08)';ctx.fill()}ctx.restore();angle+=.005;requestAnimationFrame(draw)}draw();