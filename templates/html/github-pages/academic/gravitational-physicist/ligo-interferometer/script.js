const c=document.getElementById('rippleCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let t=0;function draw(){ctx.clearRect(0,0,c.width,c.height);for(let i=0;i<8;i++){ctx.beginPath();const r=60+i*35+Math.sin(t+i)*15;ctx.arc(c.width/2,c.height/2,r,0,Math.PI*2);ctx.strokeStyle='rgba(139,92,246,0.2)';ctx.stroke()}t+=.03;requestAnimationFrame(draw)}draw();