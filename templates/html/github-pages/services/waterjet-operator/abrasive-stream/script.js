const c=document.getElementById('jetCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const spray=Array.from({length:50},()=>({x:c.width/2,y:0,vx:(Math.random()-.5)*4,vy:Math.random()*15+10}));function draw(){ctx.clearRect(0,0,c.width,c.height);spray.forEach(p=>{p.y+=p.vy;p.x+=p.vx;if(p.y>c.height){p.y=0;p.x=c.width/2}ctx.beginPath();ctx.arc(p.x,p.y,1.5,0,Math.PI*2);ctx.fillStyle='rgba(6,182,212,0.5)';ctx.fill()});requestAnimationFrame(draw)}draw();