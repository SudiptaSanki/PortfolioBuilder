const c=document.getElementById('smokerCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const plume=Array.from({length:30},()=>({x:c.width/2+(Math.random()-.5)*50,y:c.height,r:Math.random()*10+5,vy:Math.random()*2+1}));function draw(){ctx.clearRect(0,0,c.width,c.height);plume.forEach(p=>{p.y-=p.vy;p.r+=.2;if(p.y<0){p.y=c.height;p.r=Math.random()*10+5}ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(234,88,12,0.15)';ctx.fill()});requestAnimationFrame(draw)}draw();