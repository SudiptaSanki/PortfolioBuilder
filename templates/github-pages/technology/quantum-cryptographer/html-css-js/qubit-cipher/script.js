const c=document.getElementById('lattice'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();const pts=[];const sp=50;function init(){pts.length=0;for(let x=0;x<c.width+sp;x+=sp)for(let y=0;y<c.height+sp;y+=sp)pts.push({ox:x,oy:y,x,y})}init();window.addEventListener('resize',init);let mx=0,my=0;document.addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY});function draw(){ctx.clearRect(0,0,c.width,c.height);pts.forEach(p=>{const dx=mx-p.ox,dy=my-p.oy,d=Math.sqrt(dx*dx+dy*dy);const f=Math.max(0,1-d/200)*8;p.x=p.ox+dx/d*f||p.ox;p.y=p.oy+dy/d*f||p.oy;ctx.beginPath();ctx.arc(p.x,p.y,1.5,0,Math.PI*2);ctx.fillStyle='rgba(99,102,241,0.5)';ctx.fill()});requestAnimationFrame(draw)}draw();