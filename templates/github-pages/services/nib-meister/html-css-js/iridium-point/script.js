const c=document.getElementById('inkCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let t=0;function draw(){ctx.clearRect(0,0,c.width,c.height);ctx.beginPath();ctx.arc(c.width/2+Math.sin(t)*50,c.height/2+Math.cos(t)*50,150,0,Math.PI*2);ctx.fillStyle='rgba(29,78,216,0.2)';ctx.fill();t+=.01;requestAnimationFrame(draw)}draw();