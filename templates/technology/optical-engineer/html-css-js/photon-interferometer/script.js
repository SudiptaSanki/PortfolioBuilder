const c=document.getElementById('laserCanvas'),ctx=c.getContext('2d');function resize(){c.width=window.innerWidth;c.height=window.innerHeight}window.addEventListener('resize',resize);resize();let t=0;function draw(){ctx.clearRect(0,0,c.width,c.height);ctx.beginPath();for(let x=0;x<c.width;x+=4){let y=c.height/2+Math.sin(x*.02+t)*40+Math.sin(x*.05+t*2)*15;x===0?ctx.moveTo(x,y):ctx.lineTo(x,y)}ctx.strokeStyle='rgba(0,240,255,0.6)';ctx.lineWidth=1.5;ctx.stroke();t+=.04;requestAnimationFrame(draw)}draw();
document.addEventListener('mousemove',e=>{const beam=document.querySelector('.laser-beam');beam.style.left=e.clientX+'px'});